# WonderSeed Client

- ### Development Server
   ```bash
   yarn dev
   ```

- ### Production Build
   ```bash
   yarn build
   yarn preview
   ```

## Notes

Make sure to set the `VITE_API_URL` environment variable in the appropriate .env.* file(s).